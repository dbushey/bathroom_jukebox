


<%= @track.name %> 
<% if @track.explicit == true %>
    <%= '(E)' %> 
<% end %>
by 
<% @track.artists.each do |artist| %>
    <%= artist.name %>
<% end%><br>
<%= @track.album.name %>

<%= button_to 'Request Song', track_path %>
<%= button_to 'Back to Songs', tracks_path, method: 'get' %>